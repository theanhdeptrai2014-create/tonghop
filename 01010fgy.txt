-- TAZz Hub UI System - Minimalist Circle Toggle
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Màu sắc Tím Đậm & Nhạt (Neon)
local COLORS = {
    MainNeon = Color3.fromRGB(190, 0, 255),    -- Tím sáng Neon
    MainDark = Color3.fromRGB(30, 0, 60),      -- Tím đậm
    BgBlack = Color3.fromRGB(15, 15, 18),      -- Nền đen Charcoal
}

local screenGui = Instance.new("ScreenGui", playerGui)
screenGui.Name = "TAZzHub_Minimal"
screenGui.ResetOnSpawn = false

-- ============================================
-- NÚT MỞ HÌNH TRÒN (ONLY CIRCLE WITH IMAGE)
-- ============================================
local toggleButton = Instance.new("ImageButton") -- Dùng ImageButton để làm sạch
toggleButton.Name = "CircleToggle"
toggleButton.Size = UDim2.new(0, 55, 0, 55)
toggleButton.Position = UDim2.new(0, 15, 0.5, -27)
toggleButton.BackgroundColor3 = COLORS.BgBlack
toggleButton.BorderSizePixel = 0
toggleButton.AutoButtonColor = false
toggleButton.Parent = screenGui

-- Làm nút thành hình tròn
local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(1, 0)
toggleCorner.Parent = toggleButton

-- Viền Neon Tím
local toggleStroke = Instance.new("UIStroke")
toggleStroke.Color = COLORS.MainNeon
toggleStroke.Thickness = 2.5
toggleStroke.Parent = toggleButton

-- Ảnh đại diện bên trong hình tròn
local avatarImg = Instance.new("ImageLabel")
avatarImg.Size = UDim2.new(1, 0, 1, 0) -- Chiếm toàn bộ hình tròn
avatarImg.BackgroundTransparency = 1
avatarImg.Image = "rbxassetid://138193155303205" -- Ảnh của The Anh
avatarImg.Parent = toggleButton

local avatarCorner = Instance.new("UICorner")
avatarCorner.CornerRadius = UDim.new(1, 0)
avatarCorner.Parent = avatarImg

-- ============================================
-- GIAO DIỆN CHÍNH (MAIN DASHBOARD)
-- ============================================
local mainDashboard = Instance.new("Frame")
mainDashboard.Name = "MainHub"
mainDashboard.Size = UDim2.new(0, 580, 0, 380)
mainDashboard.Position = UDim2.new(0.5, -290, 0.5, -190)
mainDashboard.BackgroundColor3 = COLORS.BgBlack
mainDashboard.Visible = false -- Ẩn lúc đầu
mainDashboard.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainDashboard

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = COLORS.MainNeon
mainStroke.Thickness = 2
mainStroke.Parent = mainDashboard

-- Header với nút X fix lỗi
local header = Instance.new("Frame")
header.Size = UDim2.new(1, 0, 0, 40)
header.BackgroundTransparency = 1
header.Parent = mainDashboard

local headerTitle = Instance.new("TextLabel")
headerTitle.Text = "TAZz Hub | THE ANH"
headerTitle.Font = Enum.Font.GothamBold
headerTitle.TextColor3 = COLORS.MainNeon
headerTitle.TextSize = 18
headerTitle.Position = UDim2.new(0, 15, 0, 0)
headerTitle.Size = UDim2.new(0, 200, 1, 0)
headerTitle.BackgroundTransparency = 1
headerTitle.TextXAlignment = Enum.TextXAlignment.Left
headerTitle.Parent = header

local closeBtn = Instance.new("TextButton")
closeBtn.Name = "CloseButton"
closeBtn.Text = "X"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextColor3 = Color3.fromRGB(255, 50, 50)
closeBtn.Size = UDim2.new(0, 35, 0, 35)
closeBtn.Position = UDim2.new(1, -40, 0, 2)
closeBtn.BackgroundTransparency = 1
closeBtn.Parent = header

-- ============================================
-- LOGIC HOẠT ĐỘNG (FIX X & TOGGLE)
-- ============================================

-- Bấm X chỉ ẩn menu, không xóa
closeBtn.MouseButton1Click:Connect(function()
    mainDashboard.Visible = false
    -- Hiệu ứng nút tròn hiện ra rõ hơn khi đóng menu
    TweenService:Create(toggleButton, TweenInfo.new(0.3), {ImageTransparency = 0}):Play()
end)

-- Bấm hình tròn để mở/tắt
toggleButton.MouseButton1Click:Connect(function()
    mainDashboard.Visible = not mainDashboard.Visible
    
    -- Hiệu ứng nhấn nút
    local bounce = TweenService:Create(toggleButton, TweenInfo.new(0.1, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 48, 0, 48)})
    bounce:Play()
    bounce.Completed:Wait()
    TweenService:Create(toggleButton, TweenInfo.new(0.1), {Size = UDim2.new(0, 55, 0, 55)}):Play()
end)

-- Chức năng kéo thả (Draggable) cho cả hình tròn và Menu
local function makeDraggable(obj)
    local dragging, dragInput, dragStart, startPos
    obj.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = obj.Position
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            obj.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    obj.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

makeDraggable(toggleButton)
makeDraggable(mainDashboard)

print("TAZz Hub: Minimalist Circle Toggle Loaded!")
